# 数据库格式更新

## 更新内容

数据库中存储的文件名格式已更新为统一的格式。

### 旧格式
```
45-8-6-5-4-1-12          (t=1 时)
45-8-6-5-4-t4-1-15       (t>1 时)
```

### 新格式
```
45-8-6-5-4-1-12 (at least 1)
45-8-6-5-4-1-15 (at least 4)
```

## 格式说明

完整格式: `m-n-k-j-s-run-groups (at least t)`

组成部分:
- `m`: 总体大小 (固定为 45)
- `n`: 样本大小 (7-14)
- `k`: 组大小 (固定为 6)
- `j`: 测试大小
- `s`: 阈值
- `run`: 运行次数（相同参数的第几次运行）
- `groups`: 解的组数
- `(at least t)`: 覆盖次数（放在最后，t=1 也显示）

## 示例

| 参数 | 文件名 | 说明 |
|------|--------|------|
| m=45, n=8, k=6, j=5, s=4, t=1 | `45-8-6-5-4-1-12 (at least 1)` | 第1次运行，12个组 |
| m=45, n=8, k=6, j=5, s=4, t=4 | `45-8-6-5-4-1-15 (at least 4)` | 第1次运行，15个组 |
| m=45, n=14, k=6, j=6, s=3, t=1 | `45-14-6-6-3-2-8 (at least 1)` | 第2次运行，8个组 |
| m=45, n=14, k=6, j=6, s=3, t=4 | `45-14-6-6-3-3-20 (at least 4)` | 第3次运行，20个组 |

## 修改的文件

- `app_core.py`: 更新 `format_result_filename()` 函数
- `database.py`: 使用更新后的格式函数（无需修改）
- `app_clean.py`: 使用更新后的格式函数（无需修改）

## 向后兼容性

- 旧数据库中的记录保持不变
- 新记录将使用新格式
- 两种格式可以共存

## 注意事项

1. **t=1 也显示**: 即使 t=1，也会显示 `(at least 1)`，保持格式统一
2. **括号和空格**: 格式中包含括号和空格，解析时需要注意
3. **数据库字段**: t 仍然作为独立字段存储，filename 只是显示格式
