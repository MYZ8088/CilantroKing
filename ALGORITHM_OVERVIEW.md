# 当前程序算法说明

## 1. 这个程序实际在求解什么问题

GUI 会先从外层总体 `1..m` 中选出一个大小为 `n` 的样本池。
在这一步之后，求解器真正处理的对象其实只剩下这 `n` 个样本。

所以内部优化问题是：

- 从这 `n` 个样本中选择若干个大小为 `k` 的子集
- 使得每一个大小为 `j` 的子集
- 都能和至少一个已选的 `k` 子集有不少于 `s` 个公共元素
- 同时希望已选 `k` 子集的总数量尽可能少

在代码里，这个问题被记作 `L(n, k, j, s)`。

这里有一个很重要的点：

- `m` 只影响 GUI 中可选的真实样本编号范围
- 求解器本身只使用 `n, k, j, s`
- 最终显示结果时，才把内部下标映射回真实样本编号

## 2. 内部表示方式

求解器把组合都编码成 32 位位掩码。

- 每个目标 `j` 子集存进 `target_masks`
- 每个候选 `k` 子集存进 `cand_masks`

由于这里 `n <= 25`，一个 `uint32` 就足够表示一个子集。

这样做的好处是：

- 求两个集合交集可以直接按位与
- 求交集大小可以直接做向量化 popcount

这也是当前实现速度的底层基础。

## 3. 求解器内部的两类对象

整个问题在求解器里被拆成两类组合对象：

- 目标集：所有 `C(n, j)` 个必须被覆盖的 `j` 子集
- 候选集：所有 `C(n, k)` 个允许被选择的 `k` 子集

算法本质上就是：

从候选集中尽量少地挑出一些候选组，使得所有目标集都被覆盖。

## 4. 预处理策略

程序不会对所有实例都使用同一种预处理方式，而是根据规模切换。

### 4.1 小中规模实例

如果候选和目标之间的完整覆盖关系还在可承受范围内，就直接建立：

- `cov_table[candidate] -> 这个候选能覆盖哪些目标`
- `inv_table[target] -> 哪些候选可以覆盖这个目标`

这样后面就能走精确的增量式贪心。

### 4.2 特殊情形 `s == j`

当 `s == j` 时，覆盖关系退化为纯包含关系。
这时候可以用每个候选内部 `j` 子集表来做更快的处理。

### 4.3 大规模实例

如果完整覆盖表太大，不适合直接建立，就改用轻量级缓存：

- `cand_has_elem`
- `target_has_elem`

它们只记录候选或目标是否包含某个元素，后面用于启发式打分。

## 5. 主求解流程

整体控制流程大致是：

1. 对超大实例可先构造一个快速合法 seed
2. 运行一轮或多轮贪心构造
3. 对构造出的合法解做后处理压缩
4. 持续维护当前最好的合法解
5. 最后返回 best-so-far

求解器内部会维护两个很关键的量：

- `best`：当前找到的最好合法解
- `first_legal_elapsed`：第一次拿到合法解所花的时间

这个设计和后面的超时保底机制直接相关。

## 6. 贪心构造部分

当前程序有两条贪心主路径。

### 6.1 增量式精确贪心

当完整覆盖表已经建立好时，走这条路径。

流程是：

1. 初始时所有目标都记为未覆盖
2. 每个候选的分数就是它当前能覆盖多少个未覆盖目标
3. 每轮选择分数最高的候选
4. 把这个候选新覆盖到的目标标记掉
5. 只更新那些受影响候选的分数，而不是全量重算
6. 重复直到全部目标都被覆盖

这条路径的特点是：

- 每一步选择都比较精确
- 更新开销相对低
- 对小中规模问题效果比较稳

### 6.2 大实例启发式贪心

当实例过大，完整覆盖表不划算时，走这条路径。

它的思路不是对所有候选都做完整精确评分，而是先缩小候选范围。

流程是：

1. 统计当前仍未覆盖目标中，每个元素出现了多少次
2. 用这些频率给每个候选计算一个启发式分数
3. 只保留分数最高的 `top_k` 个候选
4. 再对这批 `top_k` 候选做精确覆盖计数
5. 从里面选真正最好的那个
6. 重复这个过程

这一块是当前大实例加速的核心。

它的本质思想是：

- 不对所有候选和所有剩余目标做全量精确评估
- 先用便宜的启发式缩小搜索范围
- 再在缩小后的集合里做精确比较

## 7. 批量评分与 GPU

在大实例里，最重的一步通常是候选批量评分。
当前实现对这一步做了 CPU / GPU 两套实现。

### 7.1 CPU 批量评分

CPU 路径按批次计算：

- 候选批次 和 目标批次做按位与
- 判断交集是否满足覆盖条件
- 统计每个候选能覆盖多少个目标

### 7.2 GPU 批量评分

如果环境支持 CuPy，并且问题规模达到阈值，就会把这一步放到 GPU 上做。

但要注意：

- GPU 不是全程参与求解
- 它只参与大批量候选评分这一段
- 如果 GPU 路径失败，会自动回退到 CPU

所以当前程序更准确的说法是：

不是“全程 GPU 求解”，而是“在大实例的重评分阶段按条件使用 GPU 加速”。

## 8. 贪心之后的后处理

贪心通常先给出一个合法解，但这个解里常常有冗余组，所以还要继续压缩。

### 8.1 局部删冗余

当前实现有两种局部删减方式。

对于较小的解：

- 直接尝试删除第 `i` 个组
- 如果删掉后仍然合法，就保留删除

对于较大的解：

- 先计算每个目标当前被多少个已选组覆盖
- 只有当某个组覆盖到的所有目标都至少还被别的组覆盖时，才允许删除它

这是后处理里最主要的一步。

### 8.2 Swap 改进

在规模不太大时，程序还会尝试：

- 删掉一个已选组
- 找一个替代候选
- 如果替换后仍然合法，就接受这次替换

这一步只在较小实例上开启，因为它代价比较高。

### 8.3 模拟退火

当解规模更小一些时，程序还可能进入模拟退火阶段。

它的基本做法是：

- 随机替换当前解中的一个组
- 检查替换后是否仍然合法
- 在一定概率下接受不那么好的扰动
- 再尝试重新删减

这一段的目标主要是跳出局部最优，而不是负责构造第一个可行解。

## 9. 大实例保底与 best-so-far 机制

这是当前版本里最重要的新增逻辑之一。

### 9.1 快速合法 seed

对于很大的实例，程序在进入完整贪心之前，可以先构造一个快速合法解。

这个 seed 的质量通常不是最好，但好处是：

- 很快就能拿到一个合法解
- 之后即使长时间优化，也不会处于“还没有任何可返回答案”的状态

### 9.2 部分解的快速补全

如果贪心中途被打断，或者后期剩余目标已经不多，程序会尝试快速补尾。

补全逻辑是：

1. 取一个当前未覆盖的目标
2. 构造一个能覆盖它的规范化 `k` 子集
3. 补入额外元素时，优先选择在剩余未覆盖目标中出现更频繁的元素
4. 把这个候选加入解中
5. 重复直到所有目标都被覆盖

这个机制不是读取一个预存答案。
它是现场根据当前未覆盖情况继续构造合法解。

### 9.3 始终维护当前最优合法解

求解器在整个运行过程中，会持续维护 `best`。

也就是说：

- 只要出现更小的合法解，就更新 `best`
- 如果收到取消信号，就尽量返回当前最好的合法解
- 不会把一个未完成的、非法的部分贪心状态直接当成最终答案

这正是你想要的“大实例即使没完全优化完，也能返回 best-so-far 合法解”的基础。

## 10. 结果验证

在返回最终结果之前，程序会对所有目标重新做一次覆盖验证。

只有当每个目标都满足题意里的覆盖要求时，这个结果才会被标记为合法。

所以当前返回结果的原则是：

- 不能只是“看起来差不多”
- 必须是对全部目标都验证通过的完整合法解

## 11. 当前超时机制的真实状态

这里要区分“求解器能力”和“GUI 接线状态”。

### 11.1 求解器层

求解器本身已经支持 deadline-safe 的返回方式，因为它接受 `cancel_fn`。
只要外部在合适的时候让 `cancel_fn` 返回 `True`，求解器就会停下来，并返回当前 best-so-far 合法解。

### 11.2 评测层

当前评测脚本里，已经把时间限制接进去了。
也就是说在 `eval.py` 这样的评测入口中，程序可以按时限触发取消，再返回当前最好合法解。

### 11.3 GUI 层

但当前 GUI 里，自动 100 秒截止还没有真正接好。

现在 GUI 的取消主要还是：

- 用户手动点击 Cancel

所以当前状态准确地说是：

- 求解器内部已经有 best-so-far 保底能力
- 评测流程里已经能按 timeout 触发它
- GUI 主入口还没有完全接上“自动 100 秒停止并返回”的逻辑

## 12. 为什么现在算法会设计成这样

当前实现不是单一算法，而是一套分层流程，原因是它要同时兼顾三件事：

1. 正确性优先
2. 大实例尽早拿到第一个合法解
3. 如果还有时间，再继续压缩解的规模

所以它最终形成了这样一条管线：

- 小实例尽量走更精确的贪心
- 大实例先靠启发式缩小评分范围
- 批量评分阶段按条件使用 GPU
- 构造完后继续删冗余和局部优化
- 超大实例随时保留一个可以拿来返回的合法 best-so-far

## 13. 一句话总结

如果只用一句话来概括当前程序的算法，就是：

它把问题建模成“用尽量少的 `k` 子集覆盖全部 `j` 子集”的组合覆盖问题，内部使用位掩码表示组合，在小实例上走精确增量贪心，在大实例上走启发式筛选加精确批评分，并在后处理阶段做删冗余与局部优化，同时始终尽量维护一个可随时返回的合法 best-so-far 解。
