# 算法优化策略与终止条件说明

## 问题：如何确定返回的解是"最优"的？

### 简短回答
**算法返回的是"启发式最优解"，而非数学证明的全局最优解。**

这是一个 NP-hard 问题，找到全局最优解在计算上是不可行的（可能需要数年时间）。因此，算法使用多种启发式策略在合理时间内找到"足够好"的解。

**重要：算法本身不知道理论下界或LJCR数据！** 这些数据仅用于事后评估解的质量。

---

## 算法策略详解

### 1. 多次尝试 (Multi-Attempt Strategy)

```python
num_att = self._effective_attempts()  # 通常是 1-5 次

for attempt in range(1, num_att + 1):
    sol = self._greedy(randomize=attempt > 1)  # 贪心构造
    sol = self._local_search(sol)              # 局部搜索
    sol = self._swap_improve(sol)              # 交换改进
    sol = self._sa_improve(sol)                # 模拟退火
    
    if len(sol) < len(best):
        best = sol  # 保留最好的解
```

**关键点：**
- 第一次尝试：确定性贪心（快速）
- 后续尝试：随机化贪心（探索不同路径）
- 每次尝试都会优化，保留最好的结果
- **算法不知道"最优值"是多少，只是尽力找更少的组**

### 2. 早停策略 (Early Stopping)

```python
if attempt >= 2 and stagnant >= 1 and (problem_is_large):
    break  # 如果连续没有改进，提前停止
```

**为什么能快速返回（如 0.2s）？**

1. **问题规模小**：
   - 小问题（n≤10）可能在第一次贪心就找到很好的解
   - 局部搜索快速移除冗余组
   
2. **连续无改进**：
   - 如果第2次尝试没有改进，且问题较大，就停止
   - 避免浪费时间在微小改进上
   - **注意：算法不知道是否达到最优，只是没有进一步改进了**

3. **自适应时间预算**：
   - 根据问题规模分配不同的优化时间
   - 小问题快速返回，大问题多花时间

### 3. 自适应尝试次数

```python
def _effective_attempts(self) -> int:
    if self._interaction_scale >= 900_000_000:
        return 1  # 超大问题：只尝试1次
    if self._interaction_scale >= 120_000_000:
        return 2  # 大问题：尝试2次
    return 5      # 小问题：尝试5次
```

**问题越大，尝试次数越少**，因为：
- 每次尝试的成本很高
- 改进空间有限
- 需要在时间和质量间平衡

---

## 理论保证（仅用于事后评估）

**重要说明：以下理论值不在算法运行时使用，仅用于评估结果质量！**

### 1. Schönheim 下界（数学证明）

对于 containment 问题（s == j），有递归下界：

```
C(n,k,t) >= ceil(n/k * C(n-1, k-1, t-1))
```

**事后评估：**
- 如果算法找到的解 == 下界 → 数学证明的最优解
- 如果算法找到的解 > 下界 → 可能还能改进（但算法已尽力）

例如：
- C(8, 6, 4) 的 Schönheim 下界 = 7
- 如果算法返回 7 组 → 最优！
- 如果算法返回 8 组 → 可能还能改进到 7

### 2. LJCR 已知最优值（仅用于测试）

La Jolla Covering Repository 收录了186个已知最优或最佳已知值：

```python
LJCR[(8, 6, 4)] = 7  # 已证明最优
LJCR[(10, 6, 4)] = 15  # 最佳已知（可能还能改进）
```

**这些数据仅用于：**
- 测试算法性能：`python tests/test_ljcr_dataset.py`
- 评估解的质量：`python eval.py --suite core`
- **不在 solver.py 运行时使用！**

---

## 为什么有时很快（0.2s），有时很慢（10s+）？

### 快速情况（0.2s - 2s）

1. **问题规模小**：
   - n ≤ 10, k ≤ 6
   - 候选数 < 1000
   - 目标数 < 500

2. **贪心效果好**：
   - 第一次贪心就接近最优
   - 局部搜索快速收敛
   - 第2次尝试没有改进 → 早停

3. **碰巧达到最优**：
   - 虽然算法不知道最优值
   - 但找到的解恰好是最优的
   - 事后对比 LJCR 数据可以确认

### 慢速情况（5s - 30s）

1. **问题规模大**：
   - n ≥ 15, k = 7
   - 候选数 > 10,000
   - 目标数 > 5,000

2. **需要多次尝试**：
   - 第一次贪心不够好
   - 需要随机化探索
   - 模拟退火耗时

3. **持续有改进**：
   - 每次尝试都能找到更好的解
   - 不会早停
   - 用完所有尝试次数

---

## 实际例子

### 例子 1：快速返回（0.2s）

```
参数: n=8, k=6, j=4, s=4
算法结果: 7 组 (0.2s)
事后评估: Schönheim下界 = 7 → 最优解！
```

**算法运行过程：**
1. 第1次贪心：找到 7 组
2. 局部搜索：无法移除任何组
3. 第2次贪心：还是 7 组（无改进）
4. 早停：连续无改进，停止
5. 返回：7 组

**算法不知道 7 是最优的，只是无法再改进了！**

### 例子 2：中等速度（2s）

```
参数: n=12, k=6, j=4, s=4
算法结果: 15 组 (2.1s)
事后评估: Schönheim下界 = 15 → 最优解！
```

**算法运行过程：**
1. 第1次贪心：找到 16 组
2. 局部搜索：移除1组 → 15 组
3. 第2次贪心：找到 15 组
4. 模拟退火：无法改进
5. 第3次尝试：还是 15 组
6. 早停：连续无改进
7. 返回：15 组

### 例子 3：较慢（10s+）

```
参数: n=18, k=7, j=5, s=4
算法结果: 58 组 (12.5s)
事后评估: Volume下界 ≈ 50 → 比下界多8组
```

**算法运行过程：**
1. 第1次贪心：找到 65 组
2. 局部搜索：移除5组 → 60 组
3. 第2次贪心：找到 58 组（改进！）
4. 模拟退火：无法改进
5. 用完所有尝试次数
6. 返回：58 组

**算法不知道理论下界是 50，只是尽力找到了 58 组。**

---

## 算法如何决定停止？

### 停止条件（按优先级）：

1. **用户取消**：点击 Cancel 按钮
2. **达到最大尝试次数**：
   - 小问题：5 次
   - 大问题：1-2 次
3. **早停触发**：
   - 已尝试 ≥ 2 次
   - 连续 1 次无改进
   - 且问题规模较大
4. **时间预算用完**：
   - 模拟退火有时间限制
   - 避免单次优化过长

**算法不会因为"达到最优"而停止，因为它不知道最优值是多少！**

---

## 如何判断解的质量？（事后评估）

### 1. 与理论下界比较

```python
from bounds import best_lower_bound

lower = best_lower_bound(n, k, j, s)
ratio = result.num_groups / lower

if ratio == 1.0:
    print("✓ 最优解（达到下界）")
elif ratio <= 1.1:
    print("✓ 优秀解（接近下界）")
elif ratio <= 1.3:
    print("○ 良好解")
else:
    print("△ 可接受解")
```

### 2. 与 LJCR 已知值比较

```python
from bounds import LJCR

if (n, k, j) in LJCR and s == j:
    ljcr_best = LJCR[(n, k, j)]
    if result.num_groups == ljcr_best:
        print("✓ 匹配已知最优值")
    elif result.num_groups < ljcr_best:
        print("🎉 发现新的更好解！")
    else:
        print(f"△ 比已知最优多 {result.num_groups - ljcr_best} 组")
```

### 3. 验证正确性

```python
if result.verified:
    print("✓ 解已验证（所有目标都被覆盖）")
else:
    print("✗ 解未通过验证（有bug）")
```

---

## 总结

### 算法运行时：

1. ❌ **不知道**理论最优值
2. ❌ **不使用** LJCR 数据
3. ❌ **不知道**何时达到最优
4. ✅ **只知道**当前找到的最好解
5. ✅ **尽力**通过多次尝试和优化找更好的解
6. ✅ **根据**连续无改进或时间预算决定停止

### 事后评估时：

1. ✅ 对比理论下界（Schönheim, Volume）
2. ✅ 对比 LJCR 已知值
3. ✅ 计算质量比率
4. ✅ 判断是否最优或接近最优

### 快速返回的真正原因：

1. 问题规模小 → 计算快
2. 贪心效果好 → 第一次就很好
3. 连续无改进 → 早停触发
4. **不是因为"知道达到最优"！**

### 如何获得更好的解：

1. 增加尝试次数：`num_attempts=10`
2. 多次运行，选最好的
3. 使用 benchmark 模式：`python eval.py --suite core`
4. 手动调整参数（如果你知道理论值）

---

## 相关文件

- `solver.py` - 核心算法实现（不使用理论值）
- `bounds.py` - 理论下界和 LJCR 数据（仅用于评估）
- `eval.py` - Benchmark 评估工具（对比理论值）
- `tests/test_ljcr_dataset.py` - LJCR 测试（验证算法质量）
- `SOLUTION.md` - 算法详细文档（中文）

