# 当前题目的背景大问题（问题铆钉）

## 1. 一句话铆钉
这个项目在解决一个经典组合优化问题：**如何用尽可能少的 `k` 元组，覆盖所有 `j` 元组（交集至少 `s`）**。

## 2. 题目背景（为什么会有这个问题）
在很多“抽样、分组、组合测试、覆盖验证”场景里，我们希望：

- 不漏掉任何关键组合（覆盖要完整）
- 同时控制实验/计算/测试成本（组数要尽量少）

这就形成了“覆盖完整性 vs 成本最小化”的核心矛盾，本题就是对这个矛盾的数学化建模。

## 3. 数学问题本体
程序的核心问题记为 `L(n, k, j, s)`：

- 从 `n` 个元素中选若干个大小为 `k` 的子集（组）
- 要求每一个大小为 `j` 的子集，至少与某个已选组有 `>= s` 个公共元素
- 目标：已选组数量最小

当 `s = j` 时，退化为经典覆盖设计 `C(n, k, t)`（其中 `t = j = s`）。

## 4. 这是个什么级别的难题
这是组合爆炸型问题，候选组规模是 `C(n, k)`，目标规模是 `C(n, j)`，随 `n` 增长非常快。  
因此它本质上是**难以精确全局求最优**的问题，工程上通常采用“可验证 + 高质量近优”的求解路线。

## 5. 整个程序到底在做什么
程序的总体职责不是“做一个固定公式计算器”，而是一个完整求解系统：

1. 接收参数（`m, n, k, j, s`）和样本池
2. 将问题构造成覆盖设计实例
3. 用启发式与局部优化策略搜索更小的解
4. 对结果做覆盖正确性验证
5. 输出可用解（组列表、组数量、耗时、对比基线）

其中真正的优化核心是 `n, k, j, s`；`m` 主要用于外层样本空间与界面输入语义。

## 6. 成功标准（项目层面的“胜负手”）
这个题目的成败不看“代码写了多少”，而看下面三点：

1. **正确性**：覆盖约束必须全部满足（不能漏覆盖）
2. **质量**：在正确前提下，组数越少越好
3. **效率**：在可接受时间内给出结果，并能稳定复现验证

## 7. 你可以把这份铆钉怎么用
这份文档可以作为后续所有工作的统一对齐基线：

- 讨论需求时：先对齐“我们在优化什么”
- 评估改动时：先问“正确性是否仍然成立”
- 比较方案时：用“组数/质量/耗时”三维度决策

---

相关参考：`README.md`、`TASK.md`、`SOLUTION.md`、`solver.py`。
