# Optimal Samples Selection System — 技术解析文档

## 目录

1. [问题定义](#1-问题定义)
2. [项目结构](#2-项目结构)
3. [核心算法总览](#3-核心算法总览)
4. [关键预处理：覆盖表](#4-关键预处理覆盖表)
5. [贪心策略选择逻辑](#5-贪心策略选择逻辑)
6. [策略一：增量贪心（主路径）](#6-策略一增量贪心主路径)
7. [策略二：启发式贪心（大规模回退）](#7-策略二启发式贪心大规模回退)
8. [后处理优化：局部搜索](#8-后处理优化局部搜索)
9. [后处理优化：模拟退火](#9-后处理优化模拟退火)
10. [多次尝试与随机化](#10-多次尝试与随机化)
11. [结果验证与质量测量](#11-结果验证与质量测量)
12. [算法整体优缺点分析](#12-算法整体优缺点分析)

---

## 1. 问题定义

### L(n, k, j, s) 覆盖设计

从集合 $\{1, 2, \ldots, n\}$ 中选出若干个大小为 $k$ 的子集（称为"组"），使得 $\{1,\ldots,n\}$ 中**每一个**大小为 $j$ 的子集，都与至少一个已选出的组有 **≥ s 个公共元素**。

目标是使选出的组数**尽可能少**。

**参数范围（本项目）：**

| 参数 | 含义 | 范围 |
|------|------|------|
| n | 总元素数（样本池大小） | 7–25 |
| k | 每个组的大小 | 4–7 |
| j | 检验子集大小 | s ≤ j ≤ k |
| s | 最小公共元素数（覆盖阈值） | 3–7 |

**特殊情况：包含覆盖（Containment）**

当 $s = j$ 时，条件变为"每个 j-子集必须被某个组完全包含"，等价于经典的组合覆盖设计 $C(n,k,t)$（其中 $t=j=s$）。此时具有比一般情况更高效的算法可用。

---

## 2. 项目结构

```
ai_homework/
├── solver.py          # 核心求解器（算法主体）
├── bounds.py          # 下界理论 + LJCR 已知最优值（验证用）
├── database.py        # SQLite 结果存储
├── app.py             # Tkinter GUI
├── main.py            # 程序入口
├── requirements.txt   # 依赖：numpy>=1.24
└── tests/
    ├── test_solver.py     # 8 个 PDF 示例的正确性测试
    ├── test_perf.py       # 20 个中大规模性能测试
    └── test_validation.py # 34 个案例与理论下界/LJCR对比验证
```

---

## 3. 核心算法总览

求解器采用三层结构，按顺序执行：

```
┌────────────────────────────────────────────────────────┐
│  阶段 1：贪心构造（Greedy Construction）               │
│    根据规模选择"增量贪心"或"启发式贪心"              │
│    逐步向解集中添加覆盖最多未覆盖目标的组            │
└───────────────────────┬────────────────────────────────┘
                        │
                        ▼
┌────────────────────────────────────────────────────────┐
│  阶段 2：局部搜索（Local Search）                      │
│    检查已选中的每个组是否冗余（去掉仍能覆盖全部）    │
│    删除所有冗余组，压缩解的大小                       │
└───────────────────────┬────────────────────────────────┘
                        │
                        ▼
┌────────────────────────────────────────────────────────┐
│  阶段 3：模拟退火（Simulated Annealing，中小规模）     │
│    随机替换组，尝试更进一步降低解的大小              │
│    接受少量"变差"的状态，以跳出局部最优             │
└────────────────────────────────────────────────────────┘
```

上述三个阶段在 `num_attempts` 次中重复运行，保留历史最优解。

---

## 4. 关键预处理：覆盖表

贪心算法的核心操作是：**对每个候选组快速计算它能覆盖多少个当前未覆盖的目标**。

朴素方法需要对每个候选组与所有目标做逐对比较，复杂度为 $O(|\text{cands}| \times |\text{targets}|)$，在大参数下极慢。

本项目通过预计算**覆盖表**来加速：

### `_cov_table[ci]` — 正向覆盖表

第 `ci` 个候选组覆盖的所有目标索引数组。

### `_inv_table[ti]` — 反向覆盖表

第 `ti` 个目标被哪些候选组覆盖的索引数组。

**构建方式：**

利用 numpy 的向量化位运算。对候选组与目标的掩码做 `&` 运算后：
- 包含情况（$s=j$）：检查 `(cand & target) == target`，即目标位集完全出现在候选中
- 一般情况（$s<j$）：用 popcount 检查交集大小 `>= s`

为控制内存，按 batch 处理（单批 ≤ 400 MB）。

**内存判断阈值：** 如果 $|\text{cands}| \times |\text{targets}| \leq 5 \times 10^8$，则构建完整覆盖表；否则进入回退路径。

---

## 5. 贪心策略选择逻辑

```python
mem_estimate = num_cands * num_targets

if mem_estimate <= 500_000_000:
    → 策略一：增量贪心（构建完整 cov_table / inv_table）
elif s == j:
    → 策略一变体：使用 jsub_table（包含情况的紧凑版覆盖表）
else:
    → 策略二：启发式贪心（Top-K 近似）
```

**各规模下的实际路径：**

| 参数 | num_cands | num_targets | 路径 |
|------|-----------|-------------|------|
| L(7,6,5,5) | 7 | 21 | 增量贪心 |
| L(15,6,5,5) | 3003 | 3003 | 增量贪心 |
| L(20,6,5,5) | 38760 | 15504 | 增量贪心 |
| L(25,6,5,5) | 177100 | 53130 | jsub_table（包含，紧凑） |
| L(20,6,6,4) | 38760 | 38760 | **启发式回退**（非包含，大） |
| L(25,6,6,4) | 177100 | 177100 | **启发式回退** |

---

## 6. 策略一：增量贪心（主路径）

### 算法步骤

```python
uncov = {所有目标}         # 未覆盖目标集合
scores[ci] = len(cov_table[ci])  # 初始分 = 覆盖目标总数

while uncov 非空:
    best = argmax(scores)   # 选得分最高的候选
    selected.add(best)      # 加入解集

    newly_covered = cov_table[best] ∩ uncov
    uncov -= newly_covered   # 标记为已覆盖

    for ti in newly_covered:
        for ci in inv_table[ti]:
            scores[ci] -= 1  # 增量更新：这个目标已被覆盖，不再贡献
```

### 为什么叫"增量"？

关键在于**scores 的更新是增量式的**：每次只更新受影响的候选，而不是重新扫描所有候选。

- 新覆盖 $m$ 个目标 → 只更新与这 $m$ 个目标有关的候选组分数
- 借助 `inv_table` 直接定位受影响候选，复杂度从 $O(|\text{cands}|)$ 降至 $O(\sum_{ti \in \text{newly\_covered}} |\text{inv\_table}[ti]|)$

### 来源

经典**贪心集合覆盖（Greedy Set Cover）**算法，有理论保证：
$$|\text{greedy}| \leq \ln(|\text{targets}|) \cdot |\text{OPT}| + 1$$

增量评分更新思路来自组合优化文献中的"lazy evaluation"技术。

---

## 7. 策略二：启发式贪心（大规模回退）

当预计算表内存超限时（约 5 亿条记录），使用近似方法。

### 算法步骤

```python
while uncov 非空:
    # 1. 计算元素频率：每个元素在多少个未覆盖目标中出现
    freq[e] = |{t ∈ uncov : e ∈ t}|  for e in 0..n-1

    # 2. 启发式得分：每个候选组的元素频率之和
    h[ci] = Σ freq[e] for e in cand_ci

    # 3. 选 Top-2000 候选，精确计算它们的实际覆盖数
    top_k = np.argpartition(h, -2000)[-2000:]
    scores = exact_count(top_k, uncov)

    # 4. 选得分最高的加入解集
    best = top_k[argmax(scores)]
    selected.add(best)
```

### 两步近似的意义

- **第一步（频率启发）：** $O(n \times |\text{cands}|)$，快速筛选出大概率优质候选
- **第二步（精确计算）：** 只对 Top-2000 个候选做精确计算，保证选择质量

### 来源

频率启发式是组合问题中常用的"权重贪心"（Weighted Greedy）思路，Top-K 过滤参考了大规模覆盖问题的实践做法。

---

## 8. 后处理优化：局部搜索

贪心构造完成后，解中可能存在**冗余组**（去掉后覆盖仍然完整）。

### 小规模（≤60组）：暴力删除

```python
for i in range(len(sol)):
    rest = sol without sol[i]
    if verify(rest):        # 去掉 i 后仍完全覆盖
        sol = rest          # 接受删除，重新从头扫描
```

### 大规模（>60组）：覆盖计数加速

```python
# 维护每个目标的覆盖次数
cov_count[ti] = 几个选中的组覆盖了目标 ti

for i in 逆序:
    if all(cov_count[ti] >= 2 for ti in cov_table[sol[i]]):
        # 该组覆盖的所有目标都有 >=2 个组覆盖，删掉不影响
        cov_count -= sol[i]的贡献
        del sol[i]
```

维护 `cov_count` 使得"是否冗余"判断从 $O(|\text{targets}|)$ 变成 $O(|\text{cov\_table}[i]|)$，对大解集效果显著。

---

## 9. 后处理优化：模拟退火

局部搜索只能删除冗余组，但无法"交换整体结构"。对于 **3 < |sol| ≤ 200** 的中等规模解，使用模拟退火进一步收缩。

### 算法

```python
T = 1.0  # 初始温度
while T > 0.01 and not timeout:
    i = 随机选一个位置
    new_mask = 随机选一个候选组

    sol[i] = new_mask
    if verify(sol):
        # 尝试去除冗余，得到更小解
        reduced = greedy_compress(sol)
        if len(reduced) < best:
            best = reduced   # 接受改进
        elif 条件允许:
            current = sol   # 以概率接受不降反升（跳出局部最优）
    else:
        sol[i] = old_mask  # 回滚

    T *= 0.995  # 降温
```

### 为什么要"接受更差的状态"？

贪心 + 局部搜索会陷入局部最优。模拟退火模拟物理退火过程：高温时接受较差状态（探索），低温时趋向确定性（利用），有较大概率找到全局更优解。

---

## 10. 多次尝试与随机化

```python
for attempt in 1..num_attempts:
    sol = greedy(randomize=(attempt > 1))
    sol = local_search(sol)
    sol = swap_improve(sol)
    sol = sa_improve(sol)
    best = min(best, sol, key=len)
```

- **第一次**：纯确定性贪心（最高分优先）
- **后续次**：在分数上加轻微随机扰动（±0.5），使每次产生不同解
- 保留历史最小解

对非常大的问题自动降低尝试次数：`num_cands > 100_000` 时最多 2 次。

---

## 11. 结果验证与质量测量

### 正确性验证（`_verify`）

每次求解后对结果做**完整枚举验证**：遍历所有 $\binom{n}{j}$ 个 j-子集，确认每个都与某个选中组有 $\geq s$ 个公共元素。

### 下界参照（`bounds.py`）

| 下界类型 | 适用条件 | 公式 |
|----------|----------|------|
| **体积下界（Volume Bound）** | 所有情况 | $\lceil \binom{n}{j} / \max\_\text{coverage} \rceil$ |
| **Schönheim 递归下界** | 仅包含情况 $s=j$ | $C(n,k,t) \geq \lceil \frac{n}{k} \cdot C(n-1, k-1, t-1) \rceil$ |
| **LJCR 已知最优** | 包含情况 $s=j$，查表 | 来自 La Jolla Covering Repository |

### 验证结果（34 个测试案例）

```
Verified: 34/34
Quality ratio (vs LJCR/LB): avg=1.344, best=1.000, worst=3.407
Grades: A=15 (≤110%), B=8 (≤125%), C=4 (≤150%), D=7 (>150%)
```

> D 级主要出现在非包含（$s<j$）案例，且下界本身较松，实际偏差未必这么大。

---

## 12. 算法整体优缺点分析

### 优点

| 方面 | 说明 |
|------|------|
| **理论有保证** | 贪心集合覆盖有 $O(\ln n)$ 近似比，不会产生极差的解 |
| **小规模精确** | 对参数较小的情况（如 PDF 8 个例子），全部命中已知最优值 |
| **内存自适应** | 根据规模动态选择策略，避免 OOM；600 MB 上限内安全运行 |
| **增量更新高效** | 预计算 cov/inv 表后，贪心每步只更新受影响候选，远快于重算 |
| **多层优化** | 贪心→局部搜索→SA，逐步压缩解的大小 |
| **结果可验证** | 每次求解后做完整枚举验证，保证解的正确性 |

### 缺点

| 方面 | 说明 |
|------|------|
| **非最优性** | 贪心 + SA 不保证找到全局最优，中大规模与 LJCR 可能有 10–30% 差距 |
| **大规模质量下降** | 对 `L(20,6,6,4)` 等无完整覆盖表的非包含大规模情况，质量比明显变差（~3.4x 体积下界） |
| **SA 时间不确定** | 模拟退火的效果高度依赖随机初始化和时间预算，结果有波动 |
| **参数敏感** | `num_attempts`、SA 温度、Top-K 大小等超参未针对每种参数组合调优 |
| **无精确下界** | 对非包含情况（$s<j$）只有体积下界，质量差距难以精确量化 |
| **空间换时间** | 预计算表在边界规模（如 $n=20,k=6,j=5,s=5$）需占 ~400 MB 内存 |

### 未来优化方向

1. **列生成（Column Generation）**：将问题改写为整数规划，用对偶松弛找下界，对中等规模可能找到最优解
2. **更好的 SA 邻域**：引入"多组同时替换"的邻域操作
3. **针对非包含情况的专门算法**：当前 Top-K 启发式对 $s<j$ 大规模情况效果不稳定
4. **并行化**：多次 attempt 可以并行执行（Python multiprocessing）
5. **学习型候选排序**：用历史求解经验预测哪些候选组更可能出现在最优解中
