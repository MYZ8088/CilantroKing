# T-Covering算法详解 (t > 1)

## 问题定义

**T-Covering设计问题**: L(n,k,j,s,t)

- **n**: 元素总数 (7-25)
- **k**: 每组包含的元素数 (4-7)
- **j**: 目标子集大小 (s ≤ j ≤ k)
- **s**: 需要被覆盖的子集大小 (3-7)
- **t**: 覆盖要求 - 每个j-subset至少需要t个不同的s-subset被覆盖 (t ≥ 2)

**目标**: 找到最少数量的k-subset组，使得对于每个j-subset，至少有t个不同的s-subset被至少一个组完全包含。

---

## 算法整体架构

```
┌─────────────────────────────────────────────────────────────┐
│                    TCoveringSolver.solve()                   │
│                                                              │
│  目标: 找到最少组数的t-covering解                              │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
        ┌───────────────────────────────────────┐
        │  初始化阶段 (__init__)                  │
        │  - 预计算覆盖表                         │
        │  - 构建s-subset到j-subset的映射         │
        │  - 设置时间预算和取消机制                │
        └───────────────────────────────────────┘
                            │
                            ▼
        ┌───────────────────────────────────────┐
        │  策略1: 迭代收缩算法                    │
        │  _iterative_shrink()                  │
        │  (仅用于小规模问题)                     │
        └───────────────────────────────────────┘
                            │
                            ▼
        ┌───────────────────────────────────────┐
        │  策略2: 多次贪心构造                    │
        │  循环 effective_attempts 次            │
        │  (3x num_attempts, 通常9-30次)        │
        └───────────────────────────────────────┘
                            │
                            ▼
        ┌───────────────────────────────────────┐
        │  每次尝试的流程:                        │
        │  1. _greedy_solve()      贪心构造      │
        │  2. _fast_local_search() 删除冗余      │
        │  3. _swap_local_search() 1-1交换       │
        │  4. _merge_local_search() 2-1合并      │
        └───────────────────────────────────────┘
                            │
                            ▼
        ┌───────────────────────────────────────┐
        │  返回最优解                             │
        │  SolverResult(groups, num_groups, ...) │
        └───────────────────────────────────────┘
```

---

## 问题规模分类

算法根据问题规模自动调整策略：

### 规模定义

```python
# 在 __init__ 中计算
self.num_targets = C(n, j)  # j-subset数量
self.num_cands = C(n, k)    # k-subset数量

self._is_large = self.num_cands > 10000 or self.num_targets > 5000
self._is_huge = self.num_cands > 50000 or self.num_targets > 20000
```

### 规模分类表

| 规模类型 | 候选数范围 | 目标数范围 | 典型案例 | 使用收缩? |
|---------|-----------|-----------|---------|----------|
| **小规模** | ≤ 10000 | ≤ 5000 | L(8,6,6,5,4): 28候选, 28目标 | ✅ 是 |
| **中等规模** | ≤ 10000 | ≤ 5000 | L(12,6,5,4,2): 924候选, 792目标 | ✅ 是 |
| **大规模** | 10001-50000 | 5001-20000 | L(16,7,6,5,2): 11440候选 | ✅ 是 |
| **超大规模** | > 50000 | > 20000 | L(20,7,6,5,2): 77520候选 | ❌ 否 |

### 策略调整

#### 1. 迭代收缩算法
```python
if not self._is_huge:  # 候选≤50000 且 目标≤20000
    使用迭代收缩算法
else:
    跳过收缩，直接贪心
```

**原因**: 
- 收缩算法需要从大解开始（可能包含所有候选）
- 超大规模问题的初始解太大，收缩效率低
- 验证操作在超大规模下很慢

#### 2. 尝试次数
```python
if self._is_huge:
    effective_attempts = max(5, num_attempts * 2)  # 6次
elif self._deadline_at:
    effective_attempts = max(10, num_attempts * 2)  # 10次
else:
    effective_attempts = num_attempts * 3  # 9次
```

| 规模 | 有时间预算 | 无时间预算 | 原因 |
|-----|----------|-----------|------|
| 小/中/大 | 10次 | 9次 | 多次尝试提升质量 |
| 超大 | 6次 | 6次 | 单次尝试耗时长，减少次数 |

#### 3. Top-K采样
```python
use_top_k = self._is_huge  # 仅超大规模使用
top_k_size = min(5000, self.num_cands // 10)
```

- 小/中/大规模: 遍历所有候选
- 超大规模: 只采样top 5000个候选（或10%）

### 实际案例

#### 案例1: L(8,6,6,5,4) - 小规模
```
候选数: 28
目标数: 28
is_large: False
is_huge: False
策略: 收缩✅ + 9次贪心 + 全候选遍历
结果: 10组, 0.15秒
```

#### 案例2: L(14,6,5,4,2) - 中等规模
```
候选数: 3003
目标数: 1001
is_large: False
is_huge: False
策略: 收缩✅ + 9次贪心 + 全候选遍历
预期: 较好质量, 几秒内完成
```

#### 案例3: L(18,7,6,5,2) - 大规模
```
候选数: 31824
目标数: 18564
is_large: True
is_huge: False
策略: 收缩✅ + 9次贪心 + 全候选遍历
预期: 较好质量, 可能需要数十秒
```

#### 案例4: L(20,7,6,5,2) - 超大规模
```
候选数: 77520
目标数: 38760
is_large: True
is_huge: True
策略: 跳过收缩❌ + 6次贪心 + Top-K采样
预期: 中等质量, 可能需要1-2分钟
```

---

## 核心算法详解

### 1. 初始化阶段 (`__init__`)

**目的**: 预计算所有必要的数据结构，加速后续求解

```python
初始化流程:
1. 生成所有j-subsets (目标) 和 k-subsets (候选)
2. 对每个j-subset，计算其包含的所有s-subsets
3. 对每个k-subset，计算其覆盖的所有s-subsets
4. 构建反向映射: s-subset → 包含它的j-subsets
```

**关键数据结构**:
- `target_masks`: 所有j-subset的位掩码数组
- `cand_masks`: 所有k-subset的位掩码数组
- `_s_subsets_per_j`: {j_mask → [s_masks]} 每个j包含的s列表
- `_cand_covers_s`: [候选索引 → 覆盖的s集合]
- `_s_to_j`: {s_mask → [(j_idx, s_idx)]} s到j的反向映射

**时间复杂度**: O(C(n,j) × C(j,s) + C(n,k) × 总s数)

---

### 2. 策略1: 迭代收缩算法 (`_iterative_shrink`)

**核心思想**: 从一个大的可行解出发，逐步删除冗余的组

**使用条件**: `if not self._is_huge`
- ✅ **使用收缩**: 候选数 ≤ 50000 **且** 目标数 ≤ 20000
- ❌ **跳过收缩**: 候选数 > 50000 **或** 目标数 > 20000 (超大规模问题)

```
算法流程:
┌─────────────────────────────────────────┐
│ 1. 初始化: 根据问题规模选择初始解         │
│    if num_cands > 50:                   │
│      # 中大规模: 随机子集                │
│      size = min(num_cands, max(20, targets/2)) │
│      initial = random_sample(size)      │
│    else:                                │
│      # 小规模: 所有候选                  │
│      initial = all_candidates           │
└─────────────────────────────────────────┘
              │
              ▼
┌─────────────────────────────────────────┐
│ 2. 验证初始解是否有效                    │
│    if not _fast_verify(): return None   │
└─────────────────────────────────────────┘
              │
              ▼
┌─────────────────────────────────────────┐
│ 3. 迭代删除循环                          │
│    while improved:                      │
│      - 随机打乱组的顺序                   │
│      - 尝试删除每个组                     │
│      - 如果删除后仍有效，接受删除          │
│      - 一旦删除成功，重新开始             │
└─────────────────────────────────────────┘
              │
              ▼
┌─────────────────────────────────────────┐
│ 4. 局部搜索优化                          │
│    - _swap_local_search()  1-1交换      │
│    - _merge_local_search() 2-1合并      │
└─────────────────────────────────────────┘
              │
              ▼
         返回优化后的解
```

**优势**:
- ✅ 从不同方向探索解空间（自顶向下 vs 贪心的自底向上）
- ✅ 对小规模问题效果显著（如L(8,6,6,5,4): 12组→10组）
- ✅ 避免贪心算法的局部最优陷阱

**适用场景**: 
- ✅ 小规模: 候选数 ≤ 50 (如 L(8,6,6,5,4): 28候选)
- ✅ 中等规模: 候选数 50-10000 (如 L(14,6,5,4,2): 3003候选)
- ✅ 大规模: 候选数 10000-50000 (如 L(18,7,6,5,2): 31824候选)
- ❌ 超大规模: 候选数 > 50000 或 目标数 > 20000 (跳过收缩算法)

---

### 3. 策略2: 贪心构造算法 (`_greedy_solve`)

**核心思想**: 每次选择能最大化覆盖未满足j-subset的候选

```
算法流程:
┌─────────────────────────────────────────┐
│ 初始化:                                  │
│ - selected = []           已选组         │
│ - covered_s_masks = {}    已覆盖的s集合  │
│ - j_covered_count = [0]*targets 每个j的覆盖计数 │
└─────────────────────────────────────────┘
              │
              ▼
┌─────────────────────────────────────────┐
│ 主循环: while 存在未满足的j-subset       │
└─────────────────────────────────────────┘
              │
              ▼
┌─────────────────────────────────────────┐
│ 1. 找出所有未满足的j-subsets             │
│    unsatisfied_j = where(j_covered_count < t) │
└─────────────────────────────────────────┘
              │
              ▼
┌─────────────────────────────────────────┐
│ 2. 对每个候选计算得分                    │
│    - 标准评分: _score_candidate()        │
│      计数能帮助多少未满足的j             │
│    - 加权评分: _score_candidate_weighted() │
│      优先覆盖距离目标t最远的j (gap²权重)  │
└─────────────────────────────────────────┘
              │
              ▼
┌─────────────────────────────────────────┐
│ 3. 选择策略                              │
│    - 确定性: 选择得分最高的候选           │
│    - 随机化: 从RCL中随机选择              │
│      RCL = top 20%且得分≥80%最高分       │
└─────────────────────────────────────────┘
              │
              ▼
┌─────────────────────────────────────────┐
│ 4. 更新状态                              │
│    - 将候选加入selected                  │
│    - 更新covered_s_masks                │
│    - 更新j_covered_count                │
└─────────────────────────────────────────┘
              │
              ▼
         循环直到所有j满足
```

**评分函数对比**:

| 评分方式 | 公式 | 特点 | 适用场景 |
|---------|------|------|---------|
| 标准评分 | `score = Σ(能帮助的j数量)` | 简单快速 | 确定性尝试 |
| 加权评分 | `score = Σ(gap²)` where `gap = t - count[j]` | 优先帮助最需要的j | 每3次尝试用1次 |

**随机化策略**:
- 第1次尝试: 确定性，标准评分
- 第2次尝试: 随机化，标准评分
- 第3次尝试: 随机化，加权评分
- 第4次尝试: 随机化，标准评分
- ... 循环

**时间复杂度**: O(迭代次数 × 候选数 × 平均s数)

---

### 4. 局部搜索1: 删除冗余 (`_fast_local_search`)

**核心思想**: 尝试删除每个组，如果删除后仍满足t-covering要求则接受

```
算法流程:
┌─────────────────────────────────────────┐
│ 初始化覆盖状态                           │
│ - 计算covered_s_masks                   │
│ - 计算j_covered_count                   │
└─────────────────────────────────────────┘
              │
              ▼
┌─────────────────────────────────────────┐
│ 多轮优化 (最多2轮)                       │
│ while improved and passes < 2:          │
└─────────────────────────────────────────┘
              │
              ▼
┌─────────────────────────────────────────┐
│ 对每个组i:                               │
│ 1. 检查删除组i后的影响                   │
│ 2. 对于组i覆盖的每个s-subset:            │
│    - 检查相关的j-subsets                 │
│    - 计算删除后的覆盖数                   │
│ 3. 如果所有j仍满足 ≥t 覆盖:              │
│    - 删除组i                             │
│    - 更新状态                            │
│    - 重新开始                            │
└─────────────────────────────────────────┘
              │
              ▼
         返回优化后的解
```

**优化技巧**:
- 第2轮随机打乱顺序，探索不同的删除序列
- 增量验证，避免完整重新计算
- 一旦删除成功立即重新开始

**效果**: 通常能删除1-3个冗余组

---

### 5. 局部搜索2: 1-1交换 (`_swap_local_search`)

**核心思想**: 尝试用一个更好的组替换现有的组

```
算法流程:
┌─────────────────────────────────────────┐
│ 对每个组i:                               │
└─────────────────────────────────────────┘
              │
              ▼
┌─────────────────────────────────────────┐
│ 1. 临时删除组i                           │
│    temp_solution = solution - {group_i} │
└─────────────────────────────────────────┘
              │
              ▼
┌─────────────────────────────────────────┐
│ 2. 计算删除后的覆盖状态                   │
│    - covered_s (不含组i)                 │
│    - j_covered_count (不含组i)           │
└─────────────────────────────────────────┘
              │
              ▼
┌─────────────────────────────────────────┐
│ 3. 寻找最佳替代组                        │
│    for 每个候选:                         │
│      if 候选不在temp_solution:           │
│        score = _score_candidate_weighted() │
│        记录最高分候选                     │
└─────────────────────────────────────────┘
              │
              ▼
┌─────────────────────────────────────────┐
│ 4. 尝试替换                              │
│    new_solution = temp_solution + {best} │
│    if _fast_verify(new_solution):       │
│      接受替换，重新开始                   │
└─────────────────────────────────────────┘
              │
              ▼
         返回优化后的解
```

**关键点**:
- 使用加权评分选择替代组（优先帮助最需要的j）
- 必须验证替换后的解仍然有效
- 即使组数不变，也可能改善解的质量（为后续优化创造机会）

**效果**: 改善解的结构，为2-1合并创造条件

---

### 6. 局部搜索3: 2-1合并 (`_merge_local_search`)

**核心思想**: 尝试用一个组替换两个组，减少总组数

```
算法流程:
┌─────────────────────────────────────────┐
│ 双重循环: 对每对组(i, j)                 │
│ for i in range(len(solution)):         │
│   for j in range(i+1, min(i+10, len)): │
└─────────────────────────────────────────┘
              │
              ▼
┌─────────────────────────────────────────┐
│ 1. 临时删除组i和组j                      │
│    temp = solution - {group_i, group_j} │
└─────────────────────────────────────────┘
              │
              ▼
┌─────────────────────────────────────────┐
│ 2. 计算删除后的覆盖状态                   │
│    - covered_s (不含i,j)                 │
│    - j_covered_count (不含i,j)           │
└─────────────────────────────────────────┘
              │
              ▼
┌─────────────────────────────────────────┐
│ 3. 寻找最佳单个替代组                    │
│    for 每个候选:                         │
│      if 候选不在temp:                    │
│        score = _score_candidate_weighted() │
│        记录最高分候选                     │
└─────────────────────────────────────────┘
              │
              ▼
┌─────────────────────────────────────────┐
│ 4. 尝试合并                              │
│    new_solution = temp + {best}         │
│    if _fast_verify(new_solution):       │
│      接受合并 (减少1组!)                  │
│      重新开始                            │
└─────────────────────────────────────────┘
              │
              ▼
         返回优化后的解
```

**优化技巧**:
- 限制j的搜索范围 (i+1 到 i+10)，避免O(n²)复杂度
- 优先尝试相邻的组对（可能有更多重叠）
- 一旦合并成功立即重新开始

**效果**: 这是减少组数的关键步骤，通常能减少1-2组

---

## 算法参数配置

### 尝试次数策略

```python
base_attempts = num_attempts  # 默认3

if is_huge:  # 候选数>50000 或 目标数>20000
    effective_attempts = max(5, base_attempts * 2)
elif has_time_budget:
    effective_attempts = max(10, base_attempts * 2)
else:
    effective_attempts = base_attempts * 3
```

**典型配置**:
- 小问题: 9次尝试 (3×3)
- 中等问题: 10次尝试 (max(10, 3×2))
- 大问题: 6次尝试 (max(5, 3×2))

### 时间预算管理

```python
time_budget_margin = 3.0s (n≥16) 或 1.5s (n<16)
deadline = start_time + (budget - margin)

# 每个阶段检查:
if time.time() >= deadline:
    return current_best
```

---

## 性能分析

### 时间复杂度

| 阶段 | 时间复杂度 | 说明 |
|------|-----------|------|
| 初始化 | O(C(n,j)×C(j,s) + C(n,k)×S) | S=总s-subset数 |
| 迭代收缩 | O(G² × V) | G=组数, V=验证时间 |
| 贪心构造 | O(I × C × S) | I=迭代次数, C=候选数 |
| 删除冗余 | O(G × S × J) | J=目标数 |
| 1-1交换 | O(G × C × V) | |
| 2-1合并 | O(G × C × V) | 限制搜索范围 |

### 空间复杂度

- 覆盖表: O(C × S) - 每个候选的s-subset集合
- 映射表: O(S × J) - s到j的反向映射
- 状态数组: O(J) - j的覆盖计数
- 总计: O(C×S + S×J + J)

### 实际性能

**L(8,6,6,5,4)案例**:
- 目标数: 28
- 候选数: 28
- 优化前: 12组
- 优化后: **10组** (减少16.7%)
- 时间: 0.15秒

---

## 算法优势

### 1. 多策略组合
- ✅ 迭代收缩: 自顶向下探索
- ✅ 贪心构造: 自底向上构建
- ✅ 多种评分: 标准+加权
- ✅ 随机化: 避免局部最优

### 2. 三层局部搜索
- ✅ 删除冗余: 移除不必要的组
- ✅ 1-1交换: 改善解的结构
- ✅ 2-1合并: 减少总组数

### 3. 高效数据结构
- ✅ 预计算覆盖表: 避免重复计算
- ✅ 增量更新: O(1)状态维护
- ✅ 位掩码: 快速集合操作

### 4. 自适应策略
- ✅ 根据问题规模调整尝试次数
- ✅ 大问题跳过迭代收缩
- ✅ 时间预算管理

---

## 改进历史

### 版本1: 基础贪心 (初始版本)
- 单一贪心策略
- 简单删除冗余
- L(8,6,6,5,4): 12组

### 版本2: 增强贪心 (第一次优化)
- 加权评分函数
- 1-1交换局部搜索
- L(8,6,6,5,4): 12组 (无改进)

### 版本3: 2-1合并 (第二次优化)
- 添加2-1合并
- 增加尝试次数
- L(8,6,6,5,4): 12组 (无改进)

### 版本4: 迭代收缩 (当前版本) ⭐
- **新增迭代收缩算法**
- 从不同方向探索解空间
- L(8,6,6,5,4): **10组** ✅ (减少16.7%)

---

## 使用示例

```python
from n_algorithms.shared.tcovering_solver import TCoveringSolver

# 创建求解器
solver = TCoveringSolver(
    n=8, k=6, j=6, s=5, t=4,
    num_attempts=3,           # 基础尝试次数
    time_budget_sec=120.0,    # 时间预算
    progress_cb=callback,     # 进度回调
    cancel_fn=cancel_check    # 取消检查
)

# 求解
result = solver.solve()

# 结果
print(f"Groups: {result.num_groups}")
print(f"Time: {result.elapsed:.2f}s")
print(f"Solution: {result.groups}")
```

---

## 未来改进方向

### 短期改进
1. **更智能的初始解** - 基于图着色或覆盖理论
2. **自适应参数** - 根据问题特征动态调整
3. **并行搜索** - 多线程探索不同策略

### 长期改进
1. **遗传算法** - 种群进化，适合大规模问题
2. **模拟退火** - 更强的全局搜索能力
3. **机器学习** - 学习最优策略选择

---

## 总结

当前t-covering算法通过**多策略组合**和**三层局部搜索**，在保持高效的同时显著提升了解的质量。特别是**迭代收缩算法**的引入，从不同方向探索解空间，成功突破了贪心算法的局部最优限制。

**核心创新**:
- 🎯 迭代收缩: 自顶向下 vs 贪心的自底向上
- 🎯 加权评分: 优先帮助最需要的j-subset
- 🎯 2-1合并: 直接减少组数的关键步骤
- 🎯 多次尝试: 随机化+多种策略组合

**实测效果**: L(8,6,6,5,4) 从12组优化到10组，减少16.7% ✅
