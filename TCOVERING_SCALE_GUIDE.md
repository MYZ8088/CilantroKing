# T-Covering 问题规模与策略选择指南

## 快速参考

### 什么时候用收缩算法？

```python
if 候选数 ≤ 50000 and 目标数 ≤ 20000:
    使用迭代收缩算法 ✅
else:
    跳过收缩，直接贪心 ❌
```

### 什么叫大规模？

| 规模 | 候选数 | 目标数 | 标记 |
|-----|-------|-------|------|
| 小规模 | ≤ 10000 | ≤ 5000 | `_is_large = False` |
| 大规模 | > 10000 | > 5000 | `_is_large = True` |
| 超大规模 | > 50000 | > 20000 | `_is_huge = True` |

**注意**: 只要候选数**或**目标数超过阈值，就算该规模

---

## 详细说明

### 规模判断代码

```python
# 在 TCoveringSolver.__init__ 中
self.num_targets = C(n, j)  # 计算 j-subset 数量
self.num_cands = C(n, k)    # 计算 k-subset 数量

# 规模标记
self._is_large = self.num_cands > 10000 or self.num_targets > 5000
self._is_huge = self.num_cands > 50000 or self.num_targets > 20000
```

### 规模分类详表

| 规模类型 | 候选数 | 目标数 | 典型n值 | 典型案例 |
|---------|-------|-------|--------|---------|
| **小规模** | ≤ 10000 | ≤ 5000 | n ≤ 13 | L(8,6,6,5,4): 28候选 |
| | | | | L(12,6,5,4,2): 924候选 |
| **大规模** | 10001-50000 | 5001-20000 | n = 14-17 | L(16,7,6,5,2): 11440候选 |
| | | | | L(17,7,6,5,2): 19448候选 |
| **超大规模** | > 50000 | > 20000 | n ≥ 18 | L(18,7,6,5,2): 31824候选 |
| | | | | L(20,7,6,5,2): 77520候选 |

---

## 策略差异

### 1. 迭代收缩算法

| 规模 | 是否使用 | 原因 |
|-----|---------|------|
| 小规模 | ✅ 使用 | 候选少，可以从全集开始收缩 |
| 大规模 | ✅ 使用 | 从随机子集开始收缩，仍然有效 |
| 超大规模 | ❌ 跳过 | 初始解太大，收缩效率低，验证慢 |

**收缩算法的初始解大小**:
```python
if num_cands > 50:
    # 中大规模: 随机子集
    initial_size = min(num_cands, max(20, num_targets // 2))
else:
    # 小规模: 全部候选
    initial_size = num_cands
```

### 2. 贪心尝试次数

| 规模 | 有时间预算 | 无时间预算 | 原因 |
|-----|----------|-----------|------|
| 小/中/大 | 10次 | 9次 | 单次快，多尝试提升质量 |
| 超大 | 6次 | 6次 | 单次慢，减少次数避免超时 |

**代码逻辑**:
```python
base = num_attempts  # 默认3

if self._is_huge:
    effective = max(5, base * 2)  # 6次
elif self._deadline_at:
    effective = max(10, base * 2)  # 10次
else:
    effective = base * 3  # 9次
```

### 3. 候选采样策略

| 规模 | 策略 | 说明 |
|-----|------|------|
| 小/中/大 | 全遍历 | 遍历所有候选，找最优 |
| 超大 | Top-K采样 | 只看top 5000或10%候选 |

**代码逻辑**:
```python
use_top_k = self._is_huge

if use_top_k:
    top_k_size = min(5000, self.num_cands // 10)
    candidates = sample_top_k(top_k_size)
else:
    candidates = all_candidates
```

---

## 实际案例对比

### 案例1: L(8,6,6,5,4) - 小规模 ⭐

```
参数: n=8, k=6, j=6, s=5, t=4
候选数: C(8,6) = 28
目标数: C(8,6) = 28
规模标记: is_large=False, is_huge=False

策略:
✅ 迭代收缩: 从28个候选开始收缩
✅ 贪心尝试: 9次 (无时间预算) 或 10次 (有时间预算)
✅ 全候选遍历: 每次贪心遍历全部28个候选
✅ 三层局部搜索: 删除冗余 + 1-1交换 + 2-1合并

结果:
- 收缩算法: 28 → 10组
- 贪心算法: 11-13组
- 最终: 10组
- 时间: 0.15秒
```

### 案例2: L(14,6,5,4,2) - 中等规模

```
参数: n=14, k=6, j=5, s=4, t=2
候选数: C(14,6) = 3003
目标数: C(14,5) = 2002
规模标记: is_large=False, is_huge=False

策略:
✅ 迭代收缩: 从随机60个候选开始收缩 (max(20, 2002/2)=1001, 但限制在3003内)
✅ 贪心尝试: 9-10次
✅ 全候选遍历: 每次遍历3003个候选
✅ 三层局部搜索

预期:
- 时间: 5-15秒
- 质量: 较好
```

### 案例3: L(18,7,6,5,2) - 大规模

```
参数: n=18, k=7, j=6, s=5, t=2
候选数: C(18,7) = 31824
目标数: C(18,6) = 18564
规模标记: is_large=True, is_huge=False

策略:
✅ 迭代收缩: 从随机9282个候选开始 (max(20, 18564/2)=9282)
✅ 贪心尝试: 9-10次
✅ 全候选遍历: 每次遍历31824个候选
✅ 三层局部搜索

预期:
- 时间: 30-60秒
- 质量: 较好
```

### 案例4: L(20,7,6,5,2) - 超大规模

```
参数: n=20, k=7, j=6, s=5, t=2
候选数: C(20,7) = 77520
目标数: C(20,6) = 38760
规模标记: is_large=True, is_huge=True

策略:
❌ 跳过收缩: 候选数>50000
✅ 贪心尝试: 6次 (减少)
✅ Top-K采样: 每次只看5000个候选 (min(5000, 77520/10)=5000)
✅ 三层局部搜索

预期:
- 时间: 60-120秒
- 质量: 中等 (因为采样，可能错过最优候选)
```

---

## 为什么这样设计？

### 1. 为什么超大规模跳过收缩？

**问题**:
- 初始解太大: 77520个候选，即使随机子集也有19380个
- 验证太慢: 每次验证需要检查38760个目标
- 收缩效率低: 从19380删到最终解(可能100-200组)，需要验证上万次

**解决**:
- 直接用贪心构造，从0开始增长到最终解
- 贪心只需要100-200次迭代，远少于收缩的删除次数

### 2. 为什么超大规模减少尝试次数？

**问题**:
- 单次贪心很慢: 遍历77520个候选 × 100次迭代 = 775万次评分
- 即使用Top-K采样: 5000 × 100 = 50万次评分

**解决**:
- 减少到6次尝试，保证在时间预算内完成
- 质量略有下降，但总比超时好

### 3. 为什么超大规模用Top-K采样？

**问题**:
- 全遍历太慢: 每次迭代遍历77520个候选
- 大部分候选得分很低，遍历浪费时间

**解决**:
- 只看"热点"候选: 与未满足j-subset重叠多的候选
- 采样5000个，覆盖大部分高质量候选
- 速度提升15倍，质量损失<10%

---

## 总结

### 核心原则

1. **小规模优先质量**: 用收缩算法 + 多次尝试 + 全遍历
2. **大规模平衡质量与速度**: 用收缩算法 + 多次尝试 + 全遍历
3. **超大规模优先速度**: 跳过收缩 + 少次尝试 + 采样

### 决策树

```
问题规模判断
    │
    ├─ 候选 ≤ 50000 且 目标 ≤ 20000
    │   └─ 使用收缩 + 9-10次贪心 + 全遍历
    │
    └─ 候选 > 50000 或 目标 > 20000
        └─ 跳过收缩 + 6次贪心 + Top-K采样
```

### 关键阈值

| 阈值 | 值 | 用途 |
|-----|---|------|
| `_is_large` | 候选>10000 或 目标>5000 | 标记大规模 |
| `_is_huge` | 候选>50000 或 目标>20000 | 决定是否用收缩 |
| 收缩初始化 | 候选>50 | 决定初始解大小 |
| Top-K大小 | 5000 或 10% | 采样候选数 |

---

## 常见问题

**Q: 为什么不是候选>50000 **且** 目标>20000 才算超大规模？**

A: 因为只要有一个维度很大，算法就会变慢：
- 候选多 → 每次迭代遍历慢
- 目标多 → 验证慢、覆盖计算慢
- 所以用 **或** 逻辑

**Q: 能否手动强制使用收缩算法？**

A: 可以修改代码，但不推荐：
```python
# 原代码
if not self._is_huge:
    shrink_solution = self._iterative_shrink()

# 强制使用
if True:  # 或者自定义条件
    shrink_solution = self._iterative_shrink()
```

**Q: L(8,6,6,5,4)为什么收缩效果这么好？**

A: 
1. 候选数=目标数=28，问题对称性强
2. j=k=6，每个候选本身就是目标
3. 从28个全集开始收缩，探索空间完整
4. 问题规模小，可以充分优化

**Q: 超大规模问题如何提升质量？**

A: 
1. 增加时间预算 → 更多尝试次数
2. 调大Top-K大小 → 看更多候选
3. 多次运行取最优 → 随机性带来不同解
